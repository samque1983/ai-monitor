version: "3.8"

services:
  radar:
    build: .
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
      - ./reports:/app/reports
      - ./logs:/app/logs
    network_mode: host  # needed to reach IBKR Gateway on localhost
    restart: "no"
